# Tareas Pendientes

- [ ] Build local: `cargo tauri build --target x86_64-pc-windows-msvc`
- [ ] QA end-to-end en la máquina local
- [ ] Cuando esté listo: `git tag -a vX.Y.Z` → push → GitHub Actions genera NSIS installer + Release
